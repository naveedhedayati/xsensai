# Source dependencies for xsensai.
# Compile with: uv pip compile requirements.in -o requirements.txt --generate-hashes
#
# Slice 0 deps only. Slice 4+ adds httpx/xdk; Slice 4+ adds whisper; etc.
# Each later slice updates this file and re-compiles.

mcp>=1.0
pydantic>=2.0
pyyaml>=6.0

# Test
pytest>=8.0
