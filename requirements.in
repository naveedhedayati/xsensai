# Source dependencies for xsensai.
# Compile with: uv pip compile requirements.in -o requirements.txt --generate-hashes
#
# Each slice that adds a runtime dep updates this file and re-compiles.

mcp>=1.0
pydantic>=2.0
pyyaml>=6.0

# Slice 1: card model + retrieval
python-frontmatter>=1.1
regex>=2024.0

# Slice 4: XDK ingestion (X API v2 client). Pre-1.0 — pin upper bound at the
# next major to avoid silent breaking changes in transitive surface.
xdk>=0.1.0,<1.0

# Slice 4: macOS Keychain access via Security.framework (PyObjC). Used so the
# refresh token is never exposed via argv during `security add-generic-password`
# (which would leak it to `ps -ef` for ~50ms per write — fine for single-user
# laptops, real risk for shared hosts). Cross-platform; on macOS uses
# Security.framework, on other platforms it picks the appropriate backend.
keyring>=24.0,<26.0

# Test
pytest>=8.0
pytest-asyncio>=0.24
